{
    "commands": [
        "cd /app",
        "aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 806156384263.dkr.ecr.us-east-1.amazonaws.com",
        "docker-compose stop backend",
        "docker-compose rm -f backend",
        "docker-compose pull backend",
        "docker-compose up -d backend",
        "docker ps",
        "docker exec django_backend cat /app/config/settings/production.py"
    ]
}
