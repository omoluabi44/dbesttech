{
  "DocumentName": "AWS-RunShellScript",
  "Targets": [
    {
      "Key": "tag:Name",
      "Values": [
        "django-nextjs-app-backend"
      ]
    }
  ],
  "Parameters": {
    "commands": [
      "sed -i 's|CORS_ALLOWED_ORIGINS=.*|CORS_ALLOWED_ORIGINS=http://50.17.131.90,http://localhost:3000|' /app/.env",
      "cd /app && docker-compose -f docker-compose.backend.yml restart"
    ]
  }
}
